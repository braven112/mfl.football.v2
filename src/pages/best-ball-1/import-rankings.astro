---
/**
 * Import Rankings (Best Ball #1) — thin route wrapper.
 * Shared implementation lives in components/shared/rankings-import/ImportRankingsPage.
 * Best-ball leagues share TheLeague's rankings bucket (see the BEST_BALL note
 * in src/utils/rankings-scope.ts), so anything imported here — or on
 * TheLeague's copy — feeds this league's draft-room queue and the "My Rank"
 * auto-pick source alike. There is no Custom Rankings board here to link to.
 */
import ImportRankingsPage from '../../components/shared/rankings-import/ImportRankingsPage.astro';

export const prerender = false;
---

<ImportRankingsPage title="Import Rankings | Best Ball #1" hasCustomRankings={false} />
