---
/**
 * HelpModal - Displays instructions for the Auction Price Predictor
 */

export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['help-modal', className]}>
  <div class="help-modal__overlay"></div>
  <div class="help-modal__content">
    <div class="help-modal__header">
      <h3>Auction Tool Guide</h3>
      <button class="help-modal__close" aria-label="Close" title="Close (ESC)">×</button>
    </div>

    <div class="help-modal__body">
      <section class="help-section">
        <h4>Rankings Import</h4>
        <p>
          Import your preferred rankings (e.g., from Dynasty League Football, FantasyPros) to customize the price predictions. 
          Use the <strong>"Rankings"</strong> tab to paste your data. The tool will blend these rankings with our algorithm.
        </p>
      </section>

      <section class="help-section">
        <h4>Price Interpretation</h4>
        <p>
          The predictor generates three price points for each player:
        </p>
        <ul>
          <li><strong>Min:</strong> The lowest expected winning bid.</li>
          <li><strong>Avg:</strong> The most likely market value.</li>
          <li><strong>Max:</strong> The ceiling price if bidding wars occur.</li>
        </ul>
        <p>
          Use the <strong>Dynasty Weight</strong> slider to adjust how much future value (Age) impacts the price vs. current production (Redraft).
        </p>
      </section>

      <section class="help-section">
        <h4>Franchise Tags</h4>
        <p>
          The <strong>"Franchise Tags"</strong> tab predicts which players will likely be tagged by their current teams, removing them from the available auction pool. 
          The algorithm considers team cap space, player value, and positional scarcity.
        </p>
      </section>

      <section class="help-section">
        <h4>Contract Lengths</h4>
        <p>
          In the <strong>Budget Planner</strong>, you can simulate contract offers. 
          Longer contracts generally require higher AAV (Average Annual Value). The tool recommends optimal contract structures based on player age and career trajectory.
        </p>
      </section>
      
      <section class="help-section">
        <h4>Key Terms</h4>
        <dl>
          <dt>Scarcity Multiplier</dt>
          <dd>Increases prices for positions with few available starters (e.g., if only 2 starting QBs are available).</dd>
          
          <dt>Demand Multiplier</dt>
          <dd>Adjusts prices based on how many teams have a critical need at a position and the cap space to pay for it.</dd>
        </dl>
      </section>
    </div>
  </div>
</div>

<style>
  .help-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1100; /* Above player modal */
    justify-content: center;
    align-items: center;
    padding: 1rem;
  }

  .help-modal.active {
    display: flex;
  }

  .help-modal__overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(2px);
    cursor: pointer;
  }

  .help-modal__content {
    position: relative;
    background: white;
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    width: 100%;
    max-width: 650px;
    max-height: 90vh;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    animation: modal-pop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes modal-pop {
    0% { transform: scale(0.95); opacity: 0; }
    100% { transform: scale(1); opacity: 1; }
  }

  .help-modal__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.25rem 1.5rem;
    border-bottom: 1px solid #e5e7eb;
    background: #f8fafc;
  }

  .help-modal__header h3 {
    margin: 0;
    font-size: 1.25rem;
    color: #1c497c;
  }

  .help-modal__close {
    background: none;
    border: none;
    font-size: 1.75rem;
    color: #64748b;
    cursor: pointer;
    line-height: 1;
    padding: 0 0.5rem;
    border-radius: 4px;
    transition: all 0.2s;
  }

  .help-modal__close:hover {
    background: #e2e8f0;
    color: #1e293b;
  }

  .help-modal__body {
    overflow-y: auto;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    font-size: 0.95rem;
    line-height: 1.6;
    color: #334155;
  }

  .help-section h4 {
    margin: 0 0 0.5rem 0;
    color: #1c497c;
    font-size: 1.05rem;
    border-bottom: 2px solid #e2e8f0;
    padding-bottom: 0.25rem;
    display: inline-block;
  }

  .help-section p {
    margin: 0;
  }

  .help-section ul {
    margin: 0.5rem 0 0 1.25rem;
    padding: 0;
  }

  .help-section li {
    margin-bottom: 0.25rem;
  }

  dl {
    display: grid;
    grid-template-columns: max-content 1fr;
    gap: 0.5rem 1.5rem;
    margin: 0;
  }

  dt {
    font-weight: 600;
    color: #0f172a;
  }

  dd {
    margin: 0;
  }
</style>

<script>
  function initHelpModal() {
    const modal = document.querySelector('.help-modal');
    const overlay = document.querySelector('.help-modal__overlay');
    const closeBtn = document.querySelector('.help-modal__close');
    const triggers = document.querySelectorAll('.help-modal-trigger');

    const openModal = () => {
      if (modal) modal.classList.add('active');
    };

    const closeModal = () => {
      if (modal) modal.classList.remove('active');
    };

    triggers.forEach(trigger => trigger.addEventListener('click', openModal));
    
    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    if (overlay) overlay.addEventListener('click', closeModal);

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && modal && modal.classList.contains('active')) {
        closeModal();
      }
    });
  }

  // Initialize on load and on Astro page transitions
  document.addEventListener('DOMContentLoaded', initHelpModal);
  document.addEventListener('astro:page-load', initHelpModal);
</script>
